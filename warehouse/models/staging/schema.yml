# GENERATED by `make generate` — do not edit by hand.

version: 2

models:
  - name: stg_chem_scada
    description: 'SCADA historian export; energy in MJ; known to contain malformed rows. (standardized to the canonical schema)'
    columns:
      - name: reading_id
        tests: [unique, not_null]
      - name: facility_id
        tests: [not_null]
      - name: reading_ts
        tests: [not_null]
      - name: energy_kwh
        tests: [not_null]
  - name: stg_steel_meters
    description: 'Plant electricity meters, hourly interval CSV export from the metering vendor. (standardized to the canonical schema)'
    columns:
      - name: reading_id
        tests: [unique, not_null]
      - name: facility_id
        tests: [not_null]
      - name: reading_ts
        tests: [not_null]
      - name: energy_kwh
        tests: [not_null]
  - name: stg_textile_looms
    description: 'Loom-floor energy readings pushed as JSON lines by the site gateway. (standardized to the canonical schema)'
    columns:
      - name: reading_id
        tests: [unique, not_null]
      - name: facility_id
        tests: [not_null]
      - name: reading_ts
        tests: [not_null]
      - name: energy_kwh
        tests: [not_null]
